{
  "name": "teledisko-dao",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service TelediskoDAO/dao",
    "create-local": "graph create --node http://localhost:8020/ TelediskoDAO/dao",
    "remove-local": "graph remove --node http://localhost:8020/ TelediskoDAO/dao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 TelediskoDAO/dao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}

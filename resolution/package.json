{
  "name": "resolution",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service TelediskoDAO/resolution",
    "create-local": "graph create --node http://localhost:8020/ TelediskoDAO/resolution",
    "remove-local": "graph remove --node http://localhost:8020/ TelediskoDAO/resolution",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 TelediskoDAO/resolution"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
